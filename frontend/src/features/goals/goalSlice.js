import { createSlice } from "@reduxjs/toolkit";

const goalSlice = createSlice({
  name: "goal",
  initialState: "",
  reducers: {},
});

export const {} = goalSlice.actions;
export const {} = goalSlice.reducer;
